# Frontend (exposed to the browser — safe to be public)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend only (server-side, never prefix with VITE_)
OPENROUTER_API_KEY=
# Only used by /api/delete-account.js — never exposed to the client.
SUPABASE_SERVICE_ROLE_KEY=
